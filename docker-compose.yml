services:
  api:
    build: .
    restart: unless-stopped
    environment:
      DATABASE_URL: sqlite:////app/data/mergen_quant.db
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      ADMIN_TELEGRAM_USER_IDS: ${ADMIN_TELEGRAM_USER_IDS:-}
      TELEGRAM_MODE: ${TELEGRAM_MODE:-polling}
      APP_ENV: ${APP_ENV:-production}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      MARKET_DATA_PROVIDER: ${MARKET_DATA_PROVIDER:-yfinance}
      LICENSED_MARKET_DATA_BASE_URL: ${LICENSED_MARKET_DATA_BASE_URL:-}
      LICENSED_MARKET_DATA_API_KEY: ${LICENSED_MARKET_DATA_API_KEY:-}
      LICENSED_MARKET_DATA_API_KEY_HEADER: ${LICENSED_MARKET_DATA_API_KEY_HEADER:-X-API-Key}
      LICENSED_MARKET_DATA_QUOTE_PATH: ${LICENSED_MARKET_DATA_QUOTE_PATH:-/quote/{symbol}}
      LICENSED_MARKET_DATA_OHLCV_PATH: ${LICENSED_MARKET_DATA_OHLCV_PATH:-/ohlcv/{symbol}}
      LICENSED_MARKET_DATA_MARKET_STATE_PATH: ${LICENSED_MARKET_DATA_MARKET_STATE_PATH:-/market-state}
      LICENSED_MARKET_DATA_PROVIDER_NAME: ${LICENSED_MARKET_DATA_PROVIDER_NAME:-licensed_rest}
      LICENSED_MARKET_DATA_TIMEOUT_SECONDS: ${LICENSED_MARKET_DATA_TIMEOUT_SECONDS:-10}
      XU100_SYMBOL: ${XU100_SYMBOL:-XU100.IS}
      CLOSE_SCAN_ENABLED: ${CLOSE_SCAN_ENABLED:-false}
      USER_PRICE_ALERTS_ENABLED: ${USER_PRICE_ALERTS_ENABLED:-true}
      USER_PRICE_ALERT_POLL_SECONDS: ${USER_PRICE_ALERT_POLL_SECONDS:-30}
      USER_PRICE_ALERT_DELIVERY_POLL_SECONDS: ${USER_PRICE_ALERT_DELIVERY_POLL_SECONDS:-5}
      USER_PRICE_ALERT_DEFAULT_REPEAT_SECONDS: ${USER_PRICE_ALERT_DEFAULT_REPEAT_SECONDS:-60}
      USER_PRICE_ALERT_MIN_REPEAT_SECONDS: ${USER_PRICE_ALERT_MIN_REPEAT_SECONDS:-30}
      USER_PRICE_ALERT_MAX_ACTIVE_PER_USER: ${USER_PRICE_ALERT_MAX_ACTIVE_PER_USER:-500}
      USER_PRICE_ALERT_MAX_BULK_IMPORT: ${USER_PRICE_ALERT_MAX_BULK_IMPORT:-250}
      USER_PRICE_ALERT_STALE_AFTER_SECONDS: ${USER_PRICE_ALERT_STALE_AFTER_SECONDS:-180}
      USER_PRICE_ALERT_MAX_DELIVERIES_PER_MINUTE_PER_USER: ${USER_PRICE_ALERT_MAX_DELIVERIES_PER_MINUTE_PER_USER:-10}
      USER_PRICE_ALERT_MAX_GLOBAL_DELIVERIES_PER_MINUTE: ${USER_PRICE_ALERT_MAX_GLOBAL_DELIVERIES_PER_MINUTE:-500}
      USER_PRICE_ALERT_AUDIO_ENABLED: ${USER_PRICE_ALERT_AUDIO_ENABLED:-true}
      USER_PRICE_ALERT_OCR_ENABLED: ${USER_PRICE_ALERT_OCR_ENABLED:-true}
      USER_PRICE_ALERT_OCR_LANGUAGE: ${USER_PRICE_ALERT_OCR_LANGUAGE:-tur+eng}
      USER_PRICE_ALERT_TEMP_FILE_TTL_MINUTES: ${USER_PRICE_ALERT_TEMP_FILE_TTL_MINUTES:-30}
      USER_PRICE_ALERT_MAX_IMAGE_BYTES: ${USER_PRICE_ALERT_MAX_IMAGE_BYTES:-10485760}
      USER_PRICE_ALERT_MAX_FILE_BYTES: ${USER_PRICE_ALERT_MAX_FILE_BYTES:-10485760}
      LONG_ONLY: ${LONG_ONLY:-true}
      SIGNAL_MONITOR_ENABLED: ${SIGNAL_MONITOR_ENABLED:-true}
      SIGNAL_MONITOR_INTERVAL_SECONDS: ${SIGNAL_MONITOR_INTERVAL_SECONDS:-5}
      ALLOW_DELAYED_DATA_FOR_LIVE_TRIGGER: ${ALLOW_DELAYED_DATA_FOR_LIVE_TRIGGER:-false}
      MAX_MARKET_DATA_STALENESS_SECONDS: ${MAX_MARKET_DATA_STALENESS_SECONDS:-}
      DEFAULT_RISK_PERCENT: ${DEFAULT_RISK_PERCENT:-1.0}
      MAX_POSITION_PERCENT: ${MAX_POSITION_PERCENT:-20}
      MAX_DAILY_VOLUME_PARTICIPATION_PERCENT: ${MAX_DAILY_VOLUME_PARTICIPATION_PERCENT:-1}
      DEFAULT_TP1_ALLOCATION: ${DEFAULT_TP1_ALLOCATION:-40}
      DEFAULT_TP2_ALLOCATION: ${DEFAULT_TP2_ALLOCATION:-35}
      DEFAULT_TP3_ALLOCATION: ${DEFAULT_TP3_ALLOCATION:-25}
      MOVE_STOP_TO_BREAKEVEN_AFTER_TP1: ${MOVE_STOP_TO_BREAKEVEN_AFTER_TP1:-true}
      MOVE_STOP_TO_TP1_AFTER_TP2: ${MOVE_STOP_TO_TP1_AFTER_TP2:-true}
      BACKTEST_ENTRY_MODE: ${BACKTEST_ENTRY_MODE:-next_session_level_touch}
      BACKTEST_INTRABAR_MODE: ${BACKTEST_INTRABAR_MODE:-lower_timeframe_then_conservative}
      BACKTEST_FILL_MODEL: ${BACKTEST_FILL_MODEL:-conservative_volume_limited}
      BACKTEST_LIMIT_LOCK_MODE: ${BACKTEST_LIMIT_LOCK_MODE:-conservative}
      BACKTEST_PRICE_MODE: ${BACKTEST_PRICE_MODE:-split_adjusted}
      BACKTEST_COMMISSION_RATE: ${BACKTEST_COMMISSION_RATE:-}
      BACKTEST_COMMISSION_MINIMUM: ${BACKTEST_COMMISSION_MINIMUM:-}
      BACKTEST_COMMISSION_TAX_RATE: ${BACKTEST_COMMISSION_TAX_RATE:-}
      BACKTEST_INCLUDE_DIVIDENDS: ${BACKTEST_INCLUDE_DIVIDENDS:-true}
      FUNDAMENTAL_PROVIDER: ${FUNDAMENTAL_PROVIDER:-auto}
      KAP_PROVIDER: ${KAP_PROVIDER:-disabled}
      FUNDAMENTAL_CROSS_CHECK_ENABLED: ${FUNDAMENTAL_CROSS_CHECK_ENABLED:-false}
      FUNDAMENTAL_CROSS_CHECK_STRICT: ${FUNDAMENTAL_CROSS_CHECK_STRICT:-true}
      FUNDAMENTAL_ALLOW_SECONDARY_FALLBACK: ${FUNDAMENTAL_ALLOW_SECONDARY_FALLBACK:-true}
      FUNDAMENTAL_ALLOW_YAHOO_FALLBACK: ${FUNDAMENTAL_ALLOW_YAHOO_FALLBACK:-true}
      FUNDAMENTAL_TIMEOUT_SECONDS: ${FUNDAMENTAL_TIMEOUT_SECONDS:-20}
      FINTABLES_MCP_URL: ${FINTABLES_MCP_URL:-https://evo.fintables.com/mcp}
      FINTABLES_MCP_BEARER_TOKEN: ${FINTABLES_MCP_BEARER_TOKEN:-}
      FINTABLES_MCP_TOOL_NAME: ${FINTABLES_MCP_TOOL_NAME:-}
      FINTABLES_MCP_SYMBOL_ARGUMENT: ${FINTABLES_MCP_SYMBOL_ARGUMENT:-symbol}
      KAP_REST_BASE_URL: ${KAP_REST_BASE_URL:-}
      KAP_REST_API_KEY: ${KAP_REST_API_KEY:-}
      KAP_REST_ENDPOINT_PATH: ${KAP_REST_ENDPOINT_PATH:-/fundamentals/{symbol}}
      KAP_REST_API_KEY_HEADER: ${KAP_REST_API_KEY_HEADER:-X-API-Key}
      KAP_REST_DISCLOSURES_PATH: ${KAP_REST_DISCLOSURES_PATH:-/disclosures}
      KAP_REST_DISCLOSURE_DETAIL_PATH: ${KAP_REST_DISCLOSURE_DETAIL_PATH:-/disclosureDetail/{id}}
      KAP_REST_SYMBOL_QUERY_PARAM: ${KAP_REST_SYMBOL_QUERY_PARAM:-symbol}
      GDELT_ENABLED: ${GDELT_ENABLED:-true}
      GROQ_ENABLED: ${GROQ_ENABLED:-false}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      TIMEZONE_NAME: ${TIMEZONE_NAME:-Europe/Istanbul}
    expose:
      - "8000"
    volumes:
      - mergen_data:/app/data
      - ./data_csv:/app/data_csv:ro
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 5s
      retries: 3

  bot:
    build: .
    restart: unless-stopped
    environment:
      DATABASE_URL: sqlite:////app/data/mergen_quant.db
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      ADMIN_TELEGRAM_USER_IDS: ${ADMIN_TELEGRAM_USER_IDS:-}
      TELEGRAM_MODE: ${TELEGRAM_MODE:-polling}
      APP_ENV: ${APP_ENV:-production}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      MARKET_DATA_PROVIDER: ${MARKET_DATA_PROVIDER:-yfinance}
      LICENSED_MARKET_DATA_BASE_URL: ${LICENSED_MARKET_DATA_BASE_URL:-}
      LICENSED_MARKET_DATA_API_KEY: ${LICENSED_MARKET_DATA_API_KEY:-}
      LICENSED_MARKET_DATA_API_KEY_HEADER: ${LICENSED_MARKET_DATA_API_KEY_HEADER:-X-API-Key}
      LICENSED_MARKET_DATA_QUOTE_PATH: ${LICENSED_MARKET_DATA_QUOTE_PATH:-/quote/{symbol}}
      LICENSED_MARKET_DATA_OHLCV_PATH: ${LICENSED_MARKET_DATA_OHLCV_PATH:-/ohlcv/{symbol}}
      LICENSED_MARKET_DATA_MARKET_STATE_PATH: ${LICENSED_MARKET_DATA_MARKET_STATE_PATH:-/market-state}
      LICENSED_MARKET_DATA_PROVIDER_NAME: ${LICENSED_MARKET_DATA_PROVIDER_NAME:-licensed_rest}
      LICENSED_MARKET_DATA_TIMEOUT_SECONDS: ${LICENSED_MARKET_DATA_TIMEOUT_SECONDS:-10}
      XU100_SYMBOL: ${XU100_SYMBOL:-XU100.IS}
      CLOSE_SCAN_ENABLED: ${CLOSE_SCAN_ENABLED:-false}
      USER_PRICE_ALERTS_ENABLED: ${USER_PRICE_ALERTS_ENABLED:-true}
      USER_PRICE_ALERT_POLL_SECONDS: ${USER_PRICE_ALERT_POLL_SECONDS:-30}
      USER_PRICE_ALERT_DELIVERY_POLL_SECONDS: ${USER_PRICE_ALERT_DELIVERY_POLL_SECONDS:-5}
      USER_PRICE_ALERT_DEFAULT_REPEAT_SECONDS: ${USER_PRICE_ALERT_DEFAULT_REPEAT_SECONDS:-60}
      USER_PRICE_ALERT_MIN_REPEAT_SECONDS: ${USER_PRICE_ALERT_MIN_REPEAT_SECONDS:-30}
      USER_PRICE_ALERT_MAX_ACTIVE_PER_USER: ${USER_PRICE_ALERT_MAX_ACTIVE_PER_USER:-500}
      USER_PRICE_ALERT_MAX_BULK_IMPORT: ${USER_PRICE_ALERT_MAX_BULK_IMPORT:-250}
      USER_PRICE_ALERT_STALE_AFTER_SECONDS: ${USER_PRICE_ALERT_STALE_AFTER_SECONDS:-180}
      USER_PRICE_ALERT_MAX_DELIVERIES_PER_MINUTE_PER_USER: ${USER_PRICE_ALERT_MAX_DELIVERIES_PER_MINUTE_PER_USER:-10}
      USER_PRICE_ALERT_MAX_GLOBAL_DELIVERIES_PER_MINUTE: ${USER_PRICE_ALERT_MAX_GLOBAL_DELIVERIES_PER_MINUTE:-500}
      USER_PRICE_ALERT_AUDIO_ENABLED: ${USER_PRICE_ALERT_AUDIO_ENABLED:-true}
      USER_PRICE_ALERT_OCR_ENABLED: ${USER_PRICE_ALERT_OCR_ENABLED:-true}
      USER_PRICE_ALERT_OCR_LANGUAGE: ${USER_PRICE_ALERT_OCR_LANGUAGE:-tur+eng}
      USER_PRICE_ALERT_TEMP_FILE_TTL_MINUTES: ${USER_PRICE_ALERT_TEMP_FILE_TTL_MINUTES:-30}
      USER_PRICE_ALERT_MAX_IMAGE_BYTES: ${USER_PRICE_ALERT_MAX_IMAGE_BYTES:-10485760}
      USER_PRICE_ALERT_MAX_FILE_BYTES: ${USER_PRICE_ALERT_MAX_FILE_BYTES:-10485760}
      LONG_ONLY: ${LONG_ONLY:-true}
      SIGNAL_MONITOR_ENABLED: ${SIGNAL_MONITOR_ENABLED:-true}
      SIGNAL_MONITOR_INTERVAL_SECONDS: ${SIGNAL_MONITOR_INTERVAL_SECONDS:-5}
      ALLOW_DELAYED_DATA_FOR_LIVE_TRIGGER: ${ALLOW_DELAYED_DATA_FOR_LIVE_TRIGGER:-false}
      MAX_MARKET_DATA_STALENESS_SECONDS: ${MAX_MARKET_DATA_STALENESS_SECONDS:-}
      DEFAULT_RISK_PERCENT: ${DEFAULT_RISK_PERCENT:-1.0}
      MAX_POSITION_PERCENT: ${MAX_POSITION_PERCENT:-20}
      MAX_DAILY_VOLUME_PARTICIPATION_PERCENT: ${MAX_DAILY_VOLUME_PARTICIPATION_PERCENT:-1}
      DEFAULT_TP1_ALLOCATION: ${DEFAULT_TP1_ALLOCATION:-40}
      DEFAULT_TP2_ALLOCATION: ${DEFAULT_TP2_ALLOCATION:-35}
      DEFAULT_TP3_ALLOCATION: ${DEFAULT_TP3_ALLOCATION:-25}
      MOVE_STOP_TO_BREAKEVEN_AFTER_TP1: ${MOVE_STOP_TO_BREAKEVEN_AFTER_TP1:-true}
      MOVE_STOP_TO_TP1_AFTER_TP2: ${MOVE_STOP_TO_TP1_AFTER_TP2:-true}
      BACKTEST_ENTRY_MODE: ${BACKTEST_ENTRY_MODE:-next_session_level_touch}
      BACKTEST_INTRABAR_MODE: ${BACKTEST_INTRABAR_MODE:-lower_timeframe_then_conservative}
      BACKTEST_FILL_MODEL: ${BACKTEST_FILL_MODEL:-conservative_volume_limited}
      BACKTEST_LIMIT_LOCK_MODE: ${BACKTEST_LIMIT_LOCK_MODE:-conservative}
      BACKTEST_PRICE_MODE: ${BACKTEST_PRICE_MODE:-split_adjusted}
      BACKTEST_COMMISSION_RATE: ${BACKTEST_COMMISSION_RATE:-}
      BACKTEST_COMMISSION_MINIMUM: ${BACKTEST_COMMISSION_MINIMUM:-}
      BACKTEST_COMMISSION_TAX_RATE: ${BACKTEST_COMMISSION_TAX_RATE:-}
      BACKTEST_INCLUDE_DIVIDENDS: ${BACKTEST_INCLUDE_DIVIDENDS:-true}
      FUNDAMENTAL_PROVIDER: ${FUNDAMENTAL_PROVIDER:-auto}
      KAP_PROVIDER: ${KAP_PROVIDER:-disabled}
      FUNDAMENTAL_CROSS_CHECK_ENABLED: ${FUNDAMENTAL_CROSS_CHECK_ENABLED:-false}
      FUNDAMENTAL_CROSS_CHECK_STRICT: ${FUNDAMENTAL_CROSS_CHECK_STRICT:-true}
      FUNDAMENTAL_ALLOW_SECONDARY_FALLBACK: ${FUNDAMENTAL_ALLOW_SECONDARY_FALLBACK:-true}
      FUNDAMENTAL_ALLOW_YAHOO_FALLBACK: ${FUNDAMENTAL_ALLOW_YAHOO_FALLBACK:-true}
      FUNDAMENTAL_TIMEOUT_SECONDS: ${FUNDAMENTAL_TIMEOUT_SECONDS:-20}
      FINTABLES_MCP_URL: ${FINTABLES_MCP_URL:-https://evo.fintables.com/mcp}
      FINTABLES_MCP_BEARER_TOKEN: ${FINTABLES_MCP_BEARER_TOKEN:-}
      FINTABLES_MCP_TOOL_NAME: ${FINTABLES_MCP_TOOL_NAME:-}
      FINTABLES_MCP_SYMBOL_ARGUMENT: ${FINTABLES_MCP_SYMBOL_ARGUMENT:-symbol}
      KAP_REST_BASE_URL: ${KAP_REST_BASE_URL:-}
      KAP_REST_API_KEY: ${KAP_REST_API_KEY:-}
      KAP_REST_ENDPOINT_PATH: ${KAP_REST_ENDPOINT_PATH:-/fundamentals/{symbol}}
      KAP_REST_API_KEY_HEADER: ${KAP_REST_API_KEY_HEADER:-X-API-Key}
      KAP_REST_DISCLOSURES_PATH: ${KAP_REST_DISCLOSURES_PATH:-/disclosures}
      KAP_REST_DISCLOSURE_DETAIL_PATH: ${KAP_REST_DISCLOSURE_DETAIL_PATH:-/disclosureDetail/{id}}
      KAP_REST_SYMBOL_QUERY_PARAM: ${KAP_REST_SYMBOL_QUERY_PARAM:-symbol}
      GDELT_ENABLED: ${GDELT_ENABLED:-true}
      GROQ_ENABLED: ${GROQ_ENABLED:-false}
      GROQ_API_KEY: ${GROQ_API_KEY:-}
      TIMEZONE_NAME: ${TIMEZONE_NAME:-Europe/Istanbul}
    volumes:
      - mergen_data:/app/data
      - ./data_csv:/app/data_csv:ro
    command: ["/app/docker-run-bot.sh"]
    depends_on:
      - api

volumes:
  mergen_data:
